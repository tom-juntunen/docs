---
title: "Get a User-Visible Job"
sidebarTitle: "Get Job"
description: "GET /api/open/v1/jobs/:jobId — retrieves a job from the unified user-visible feed."
---

Retrieve a job from the unified user-visible feed by ID. The feed includes Open API ingestion jobs, workspace uploads, saved runs, and one aggregate record per review. Internal records remain private. Use this endpoint to inspect status, source, job type, consumption, billing, file and page information when applicable, project metadata, and `resultUrl`.

Review aggregate records require both `jobs:read` and `reviews:read`. Without `reviews:read`, a review ID returns the normal not-found response without exposing review metadata.

## Authentication

Include your API key as `Authorization: Bearer YOUR_API_KEY` in the request headers.

## Endpoint

```
GET https://plangrep.com/api/open/v1/jobs/{jobId}
```

## Path Parameters

<ParamField path="jobId" type="string" required>
  The unique identifier of a user-visible job. It can be returned by either job-list endpoint.
</ParamField>

## Example Request

```bash
curl --request GET \
  --url https://plangrep.com/api/open/v1/jobs/job_01hy3m8qtn2e5r7k9p0w4x1def \
  --header "Authorization: Bearer YOUR_API_KEY" \
  --header "Accept: application/json"
```

## Response Fields

<ResponseField name="job" type="Job" required>
  The normalized app shape for the requested user-visible job.

  <Expandable title="Job object fields">
    <ResponseField name="id" type="string" required>
      Unique identifier for the job.
    </ResponseField>

    <ResponseField name="displayName" type="string" required>
      Human-readable display name for the job. Uses `externalId` when supplied, otherwise falls back to the project name and creation date.
    </ResponseField>

    <ResponseField name="activityLabel" type="string" required>
      Label suitable for activity feeds and dashboard rows. Currently matches `displayName`.
    </ResponseField>

    <ResponseField name="externalId" type="string | null">
      Client-supplied correlation ID, or `null` if none was provided at creation.
    </ResponseField>

    <ResponseField name="status" type="JobStatus" required>
      Current status of the job. See the **Job Status Reference** section below.
    </ResponseField>

    <ResponseField name="source" type="string">
      Origin of the job: `open_api` or `workspace`. Use `jobType` and `jobKind` to distinguish ingestion, saved-run, and review work.
    </ResponseField>

    <ResponseField name="jobType" type="string">
      Normalized app job type.
    </ResponseField>

    <ResponseField name="jobKind" type="string">
      Normalized app job kind.
    </ResponseField>

    <ResponseField name="consumption" type="object">
      Consumption information when applicable.
    </ResponseField>

    <ResponseField name="projectId" type="string" required>
      The ID of the parent project.
    </ResponseField>

    <ResponseField name="project" type="object">
      Parent project summary, when available.

      <Expandable title="project fields">
        <ResponseField name="id" type="string" required>
          Parent project ID.
        </ResponseField>

        <ResponseField name="name" type="string" required>
          Parent project name.
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="limits" type="object" required>
      Processing limits applied to this job.

      <Expandable title="limits fields">
        <ResponseField name="maxBytesPerFile" type="integer" required>
          Maximum declared bytes for one file. The default is 2 GiB.
        </ResponseField>

        <ResponseField name="maxBytesPerRun" type="integer" required>
          Maximum declared bytes across the job. The default is 10 GiB.
        </ResponseField>

        <ResponseField name="maxFilesPerRun" type="integer" required>
          Maximum files across the job. The default is 5,000.
        </ResponseField>

        <ResponseField name="maxPagesPerPdf" type="integer" required>
          Maximum pages in one PDF. The default is 3,000.
        </ResponseField>

        <ResponseField name="maxPagesPerRun" type="integer" required>
          Maximum prepared pages across the job. The default is 50,000.
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="pageCount" type="integer" required>
      Total number of pages submitted for processing.
    </ResponseField>

    <ResponseField name="successfulPageCount" type="integer" required>
      Number of pages processed without errors so far.
    </ResponseField>

    <ResponseField name="failedPageCount" type="integer" required>
      `0` unless the job reaches `completed_with_errors` or `failed`; then the number of failed pages.
    </ResponseField>

    <ResponseField name="progress" type="object" required>
      Dashboard-friendly progress summary for the job.

      <Expandable title="progress fields">
        <ResponseField name="mode" type="string" required>
          `determinate` when a counter is available; otherwise `indeterminate`.
        </ResponseField>

        <ResponseField name="phase" type="string" required>
          Current phase: `provisioning`, `upload`, `preflight`, `preparation`, `processing`, `normalizing`, `rendering`, `indexing`, `finalizing`, `complete`, or `error`.
        </ResponseField>

        <ResponseField name="message" type="string" required>
          Human-readable status message.
        </ResponseField>

        <ResponseField name="identity" type="object" required>
          Revisions for the progress counters. Reset local progress state when `overallRevision` or `stepRevision` changes.
        </ResponseField>

        <ResponseField name="overall" type="object | null" required>
          Aggregate counter with `completed`, `total`, `unit` (`files`, `pages`, `regions`, or `steps`), and `percent`. `null` when no aggregate counter is available.
        </ResponseField>

        <ResponseField name="step" type="object | null">
          Optional current-stage counter with the same fields as `overall`. Present only when `overall` is `null`.
        </ResponseField>

        <ResponseField name="percent" type="integer | null" required>
          Convenience copy of `overall.percent`, or `null` when `overall` is `null`.
        </ResponseField>

        <ResponseField name="updatedAt" type="string" required>
          ISO 8601 timestamp of the latest job update.
        </ResponseField>
      </Expandable>
    </ResponseField>

    <ResponseField name="billing" type="object" required>
      Billing snapshot for the job.
    </ResponseField>

    <ResponseField name="files" type="object[]" required>
      The first page of attached-file metadata. At most 100 files are returned inline.
    </ResponseField>

    <ResponseField name="fileCount" type="integer" required>
      Total files attached to the job.
    </ResponseField>

    <ResponseField name="filesNextCursor" type="string | null" required>
      Cursor for [List Job Files](/api-reference/jobs/list-job-files), or `null` when the inline page contains every file.
    </ResponseField>

    <ResponseField name="filesTruncated" type="boolean" required>
      Whether more files are available from [List Job Files](/api-reference/jobs/list-job-files).
    </ResponseField>

    <ResponseField name="counters" type="object">
      File-state counts for Open API ingestion jobs, including `total`, `uploaded`, `queued`, `preflighting`, `processing`, `ready`, `failed`, `cancelled`, and `blockedCredits`.
    </ResponseField>

    <ResponseField name="resultUrl" type="string">
      Bearer-authenticated Open API path for this job's result, such as `/api/open/v1/jobs/{jobId}/result`. No session-authenticated job URL, storage key, credential, signed artifact URL, or artifact download link is exposed.
    </ResponseField>

    <ResponseField name="error" type="object | null">
      Error details when status is `failed`, otherwise `null`.
    </ResponseField>

    <ResponseField name="createdAt" type="string" required>
      ISO 8601 timestamp of job creation.
    </ResponseField>

    <ResponseField name="updatedAt" type="string" required>
      ISO 8601 timestamp of the most recent status update.
    </ResponseField>

    <ResponseField name="completedAt" type="string | null">
      ISO 8601 timestamp of when the job reached a terminal state, or `null` if still in progress.
    </ResponseField>

    <ResponseField name="expiresAt" type="string" required>
      ISO 8601 timestamp after which the job and its result are no longer accessible.
    </ResponseField>
  </Expandable>
</ResponseField>

## Job Status Reference

### Non-Terminal Statuses
Jobs in these states are still in progress and may change status with subsequent polling.

| Status | Description |
|---|---|
| `provisioning` | Job infrastructure is being set up. |
| `awaiting_uploads` | Job is ready to receive file uploads. |
| `uploading` | Files are actively being uploaded. |
| `preflighting` | Uploaded files are being validated, classified from page evidence, and priced before processing begins. |
| `blocked_insufficient_credits` | Job is paused — add credits and call [Resume Job](/api-reference/jobs/resume-job). |
| `processing` | Plangrep is actively processing uploaded documents. |

### Terminal Statuses
Jobs in these states will not change further. Results (if any) can be retrieved.

| Status | Description |
|---|---|
| `completed` | All pages processed successfully. Result is available. |
| `completed_with_errors` | Processing finished with partial page-level errors. Partial result is available. |
| `failed` | Processing failed entirely. Check `job.error` for details. |
| `cancelled` | Job was cancelled via the [Cancel Job](/api-reference/jobs/cancel-job) endpoint. |
| `expired` | Job was not completed before its expiry time. |

## Example Response

```json
{
  "job": {
    "id": "job_01hy3m8qtn2e5r7k9p0w4x1def",
    "displayName": "order-7890",
    "activityLabel": "order-7890",
    "externalId": "order-7890",
    "source": "open_api",
    "jobType": "document_ingestion",
    "jobKind": "document_ingestion",
    "consumption": { "type": "pages", "count": 87, "drawingPages": 87, "documentPages": 0 },
    "status": "processing",
    "projectId": "proj_01hx9z4kqr8e7m3p5n6w2v0abc",
    "project": {
      "id": "proj_01hx9z4kqr8e7m3p5n6w2v0abc",
      "name": "Downtown Office Tower"
    },
    "limits": {
      "maxBytesPerFile": 2147483648,
      "maxBytesPerRun": 10737418240,
      "maxFilesPerRun": 5000,
      "maxPagesPerPdf": 3000,
      "maxPagesPerRun": 50000
    },
    "pageCount": 204,
    "successfulPageCount": 87,
    "failedPageCount": 0,
    "progress": {
      "mode": "determinate",
      "identity": { "overallRevision": "job:job_01hy3m8qtn2e5r7k9p0w4x1def:file_xyz789", "stepRevision": null },
      "overall": { "completed": 87, "total": 204, "unit": "pages", "percent": 43 },
      "percent": 43,
      "phase": "processing",
      "message": "Files are being processed.",
      "updatedAt": "2024-11-02T09:07:35.000Z"
    },
    "billing": {
      "creditsDebited": 87
    },
    "files": [
      {
        "id": "file_xyz789",
        "fileName": "architectural_drawings.pdf",
        "status": "processing"
      }
    ],
    "fileCount": 1,
    "filesNextCursor": null,
    "filesTruncated": false,
    "counters": {
      "total": 1,
      "uploaded": 1,
      "queued": 0,
      "preflighting": 0,
      "processing": 1,
      "ready": 0,
      "failed": 0,
      "cancelled": 0,
      "blockedCredits": 0
    },
    "resultUrl": "/api/open/v1/jobs/job_01hy3m8qtn2e5r7k9p0w4x1def/result",
    "error": null,
    "createdAt": "2024-11-02T09:00:00.000Z",
    "updatedAt": "2024-11-02T09:07:35.000Z",
    "completedAt": null,
    "expiresAt": "2024-11-09T09:00:00.000Z"
  }
}
```

<Tip>
  When polling for job completion, use an interval of **10–30 seconds**.
</Tip>
