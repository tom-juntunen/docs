---
title: "List Specification Documents in a Project"
sidebarTitle: "List Specs"
description: "GET /api/open/v1/projects/:projectId/specifications - paginated list of specification document rows in a project."
---

Returns a paginated list of uploaded specification documents for a project. Each row represents a specification source document, not an individual CSI section. Use the section endpoints to inspect the sections within a spec document.

## Endpoint

```
GET https://plangrep.com/api/open/v1/projects/{projectId}/specifications
```

## Path Parameters

<ParamField path="projectId" type="string" required>
  The unique identifier of the project whose specification documents you want to list.
</ParamField>

## Query Parameters

<ParamField query="limit" type="integer" default="100">
  Maximum number of records to return per page. Must be between **1** and **500**.
</ParamField>

<ParamField query="cursor" type="string">
  Pagination cursor returned as `nextCursor` from a previous response. Omit to fetch the first page.
</ParamField>

<ParamField query="compact" type="boolean">
  When `true`, artifact URL fields are stripped from each entity, reducing payload size.
</ParamField>

<ParamField query="specId" type="string">
  Restrict results to a specific specification document ID.
</ParamField>

<ParamField query="fileName" type="string">
  Case-insensitive substring match on the uploaded specification file name.
</ParamField>

<ParamField query="classification" type="string">
  Filter by parent file classification. Usually `specifications`, but mixed files can also produce specification rows.
</ParamField>

## Response

Returns a `ProjectSpecificationEntityListResponse` object.

### Pagination Fields

<ResponseField name="total" type="integer">
  Total number of specification documents matching the current filters across all pages.
</ResponseField>

<ResponseField name="limit" type="integer">
  The page size limit that was applied to this response.
</ResponseField>

<ResponseField name="cursor" type="string">
  The cursor value used for this page, echoed back from the request.
</ResponseField>

<ResponseField name="nextCursor" type="string">
  Opaque cursor to pass as `cursor` to retrieve the next page. `null` when there are no more pages.
</ResponseField>

<ResponseField name="returnedCount" type="integer">
  Number of specification documents returned in this response.
</ResponseField>

<ResponseField name="hasMore" type="boolean">
  `true` if additional pages are available beyond this response.
</ResponseField>

<ResponseField name="pageInfo" type="object">
  Relay-style page info object.

  <Expandable title="pageInfo fields">
    <ResponseField name="startCursor" type="string">
      Cursor pointing to the first record in this page.
    </ResponseField>
    <ResponseField name="endCursor" type="string">
      Cursor pointing to the last record in this page.
    </ResponseField>
    <ResponseField name="hasPreviousPage" type="boolean">
      `true` if records exist before this page.
    </ResponseField>
    <ResponseField name="hasNextPage" type="boolean">
      `true` if records exist after this page.
    </ResponseField>
  </Expandable>
</ResponseField>

### Data Fields

<ResponseField name="specifications" type="ProjectSpecificationEntity[]">
  Array of specification document rows.

  <Expandable title="ProjectSpecificationEntity fields">
    <ResponseField name="specId" type="string">
      Unique identifier for this specification document.
    </ResponseField>
    <ResponseField name="fileName" type="string">
      Original uploaded file name.
    </ResponseField>
    <ResponseField name="classification" type="string">
      File classification for the source document, typically `specifications`.
    </ResponseField>
    <ResponseField name="status" type="string">
      Current processing status for this specification document.
    </ResponseField>
    <ResponseField name="sectionCount" type="integer">
      Number of CSI-like specification sections detected in this document.
    </ResponseField>
    <ResponseField name="artifacts" type="ProjectEntityArtifact[]">
      Artifact objects for this specification document, such as `specification_file`. Omitted when `compact=true`.
    </ResponseField>
  </Expandable>
</ResponseField>

## Example Request

```bash
curl -X GET "https://plangrep.com/api/open/v1/projects/proj_abc123/specifications?limit=20&classification=specifications" \
  -H "Authorization: Bearer YOUR_API_TOKEN"
```

## Example Response

```json
{
  "total": 3,
  "limit": 20,
  "cursor": null,
  "nextCursor": null,
  "returnedCount": 3,
  "hasMore": false,
  "pageInfo": {
    "startCursor": "eyJvZmZzZXQiOjB9",
    "endCursor": "eyJvZmZzZXQiOjJ9",
    "hasPreviousPage": false,
    "hasNextPage": false
  },
  "specifications": [
    {
      "specId": "spec_001",
      "fileName": "Project_Specifications.pdf",
      "classification": "specifications",
      "status": "complete",
      "sectionCount": 48,
      "artifacts": [
        {
          "type": "specification_file",
          "url": "https://...",
          "readWith": "plangrep_show_artifact_file"
        }
      ]
    }
  ]
}
```

## Error Responses

| Status | Code | Description |
|---|---|---|
| `400` | `invalid_request` | Invalid pagination or filter value. |
| `401` | `unauthorized` | Missing or invalid Bearer token. |
| `404` | `project_not_found` | Project not found. |
